# Copy to .env and fill in values
YOUTUBE_API_KEY=
# One or more channels (comma/space-separated): @handle, UC..., or full channel URL
# Example: YOUTUBE_CHANNELS=@channel1,@channel2,UCxxxxxxxxxxxxxxxxxxxxxx
YOUTUBE_CHANNELS=
# If relative, this saves next to yt_live_link_saver.js
YOUTUBE_OUTPUT=saved_live_links.txt
YOUTUBE_INTERVAL_SECONDS=3600
# Optional: for uploads-based discovery, scan the latest N uploads (1-50)
YOUTUBE_SCAN_LATEST=50
# Optional: set true to use search.list discovery (very expensive quota)
YOUTUBE_USE_SEARCH=false

# Optional: also add saved videos to a playlist on your YouTube account (requires OAuth)
# YOUTUBE_SAVE_TO_PLAYLIST=true
# YOUTUBE_PLAYLIST_TITLE=Saved Streams
# YOUTUBE_PLAYLIST_PRIVACY=private  # private|unlisted|public
# OAuth client (Google Cloud Console -> OAuth -> Desktop app)
# YOUTUBE_OAUTH_CLIENT_ID=
# YOUTUBE_OAUTH_CLIENT_SECRET=
# Refresh token (one-time). You can either:
# - run: bun yt_live_link_saver.js --env-file .env --oauth-setup
# - OR enable playlist saving and run once interactively (it will prompt)
# - If your browser is on a different machine, copy/paste the final localhost redirect URL into the terminal when prompted
# By default it is saved to `.youtube_oauth_tokens.json` (override with YOUTUBE_OAUTH_TOKEN_FILE).
# YOUTUBE_OAUTH_REFRESH_TOKEN=
# YOUTUBE_OAUTH_TOKEN_FILE=.youtube_oauth_tokens.json
# Optional: disable automatic prompting (useful for cron/headless)
# YOUTUBE_OAUTH_AUTO_SETUP=false
# Optional: keep the playlist in sync with the output file (default: true)
# YOUTUBE_PLAYLIST_SYNC_OUTPUT=true
# Optional: queue file for retrying playlist inserts (defaults next to YOUTUBE_OUTPUT)
# YOUTUBE_PLAYLIST_QUEUE_FILE=
# Optional: state file used to decide when a full output-file sync is needed
# (defaults next to YOUTUBE_OUTPUT)
# YOUTUBE_PLAYLIST_STATE_FILE=

# Optional: verbose diagnostics (never prints tokens)
# YOUTUBE_DEBUG=true
